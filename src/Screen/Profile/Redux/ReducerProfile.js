import {SET_NEW_DATA_PROFILE} from './ActionEditProfile';

const initialState = {
  newData: [],
};

export default (state = initialState, {type, payload}) => {
  switch (type) {
    default:
      return state;
  }
};
